{% extends 'base.html.twig' %}

{% block title %}Jeux{% endblock %}

{% block body %}
    <h1>Jeux</h1>

    <section>
        <img src="/img/{{jeux.imgCover}}" alt="">
        {% for pic in jeux.imgGameplay %}
            <img src="/img/{{pic}}" alt="">
        {% endfor %}
        <h2>{{jeux.titre}}</h2>
        <h3>Edité par {{jeux.editeur}}</h3>
        <p>{{jeux.type}} sorti le {{jeux.date|date('d/m/Y')}}</p>
        <p>{{jeux.description}}</p>
    </section>

    <a href="{{ path('jeux_index') }}">back to list</a>

    <a href="{{ path('jeux_edit', {'id': jeux.id}) }}">edit</a>

    {{ include('jeux/_delete_form.html.twig') }}
{% endblock %}
