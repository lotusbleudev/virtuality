{% extends 'base.html.twig' %}

{% block title %}VIRTUALITY - Tarification{% endblock %}

{% block body %}
    <h1>Tarification</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Montant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for prix in prixes %}
            <tr>
                <td>{{ prix.id }}</td>
                <td>{{ prix.titre }}</td>
                <td>{{ prix.montant }}</td>
                <td>
                    <a href="{{ path('prix_show', {'id': prix.id}) }}"><button class="butn">Voir</button></a>
                    <a href="{{ path('prix_edit', {'id': prix.id}) }}"><button class="butn">Editer</button></a>
                    
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('prix_new') }}"><button class="butn">Créer un nouveau tarif</button></a>
{% endblock %}
