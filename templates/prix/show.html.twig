{% extends 'base.html.twig' %}

{% block title %}VIRTUALITY - Prix{% endblock %}

{% block body %}
    <h1>Prix</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prix.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ prix.titre }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ prix.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prix_index') }}"><button class="butn">Retour a la liste</button></a>

    <a href="{{ path('prix_edit', {'id': prix.id}) }}"><button class="butn">Modifier</button></a>

    {{ include('prix/_delete_form.html.twig') }}
{% endblock %}
