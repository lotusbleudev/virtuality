<header>
    <nav>
	<a class = "logo" href="{{ path("home") }}">VIRTUALITY</a>
	<p class = "ouverture">Ouvert 7j/7 - De 9h00 à 20h00</p>
	<div class="menu-button__wrapper">
		<div class="menu-button">
			<span class="menu-button__bar"></span>
			<span class="menu-button__bar"></span>
			<span class="menu-button__bar"></span>
		</div>
	</div>
    </nav>

	<div class="menu-overlay">
		<nav class="nav-login">
			<a href="{{path("app_login")}}">Connexion</a>
			<a href="{{ path("app_register") }}">Inscription</a>
		</nav>
		<nav class="nav">
			<a href="{{ path("home") }}">Accueil</a>
			<a href="{{ path("jeux_index") }}">Liste des jeux</a>
			<a href="{{ path("prix_index") }}">Tarification</a>
			<a href="{{ path("reservation_index") }}">Reservation</a>
			<a href="{{ path("tournois_index") }}">Tournois</a>
			<a href="{{ path("user_index") }}">Utilisateur</a>
		</nav>
		{# <nav class="nav">
			<p>BackOffice :</p>
			<a href="{{ path("user_index") }}">Utilisateur</a>
		</nav> #}
	</div>
</header>
