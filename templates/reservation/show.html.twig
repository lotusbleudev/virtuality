{% extends 'base.html.twig' %}

{% block title %}VIRTUALITY - Réservation{% endblock %}

{% block body %}
    <h1>Réservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reservation.date ? reservation.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Nb_joueurs</th>
                <td>{{ reservation.nbJoueurs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_index') }}"><button>Retour a la liste</button></a>

    <a href="{{ path('reservation_edit', {'id': reservation.id}) }}"><button>Editer</button></a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
