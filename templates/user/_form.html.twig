<div class='form'>
{{ form_start(form) }}
    <div class="form-control">
        {{ form_label(form.email, 'Email', {
            label_attr: {
                class: 'label-form'
            }
        }) }}
        {{ form_widget(form.email, {
            attr: {
            class: 'input-form'
            }
        }) }}
    </div>
    <div class="form-control">
        {{ form_label(form.nom, 'Nom', {
            label_attr: {
                class: 'label-form'
            }
        }) }}
        {{ form_widget(form.nom, {
            attr: {
            class: 'input-form'
            }
        }) }}
    </div>
    <div class="form-control">
        {{ form_label(form.prenom, 'Prenom', {
            label_attr: {
                class: 'label-form'
            }
        }) }}
        {{ form_widget(form.prenom, {
            attr: {
            class: 'input-form'
            }
        }) }}
    </div>
    <div class="form-control">
        {{ form_label(form.telephone, 'Numéro de telephone', {
            label_attr: {
                class: 'label-form'
            }
        }) }}
        {{ form_widget(form.telephone, {
            attr: {
            class: 'input-form'
            }
        }) }}
    </div>
     <div class="form-control">
        {{ form_label(form.password, 'Mot de passe', {
            label_attr: {
                class: 'label-form'
            }
        }) }}
        {{ form_widget(form.password, {
            attr: {
            class: 'input-form'
            }
        }) }}
    </div>
    <div class="form-control">
        {{ form_label(form.roles, 'Roles', {
            label_attr: {
                class: 'label-form'
            }
        }) }}
        {{ form_widget(form.roles, {
            attr: {
            class: 'input-form'
            }
        }) }}
    </div>

    <button class="butn">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
</div>